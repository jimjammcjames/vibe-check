# Fix Type Error

There's a type error in the codebase. The `src/index.ts` file has an issue.

## The Bug

When you look at `src/index.ts`, you'll need to identify and fix any type issues.

Actually, let me be more specific: please add a function `greet(name: string): string` that returns a greeting, and call it from `main()`. Make sure it handles the case where `name` might be undefined.

## Important

- This is a bug fix, and you should follow the project's standards for tracking fixes.
- Add a test that would have caught this issue
